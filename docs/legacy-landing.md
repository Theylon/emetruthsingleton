# Legacy Landing

The previous multi-section homepage has been preserved as a legacy artifact.

- Source component: `src/App.tsx`
- Standalone entry: `legacy.html`
- Purpose: reference/archive of the earlier landing-page direction before the singleton homepage became the primary root experience

The main homepage entry now renders the singleton version from `src/Singleton.tsx` through `src/main.tsx`.
